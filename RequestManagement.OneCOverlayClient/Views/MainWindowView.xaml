<Window x:Class="OneCOverlayClient.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OneCOverlayClient.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:OneCOverlayClient.ViewModels.Behaviors"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="450" Width="300">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="10,40,10,10">
            <TextBox materialDesign:HintAssist.Hint="МОЛ" 
                     Margin="0,0,0,20"
                     Tag="{Binding ClearSelectedFinanciallyResponsiblePersonCommand}" 
                     IsReadOnly="True" 
                     materialDesign:ValidationAssist.UsePopup="True"
                     materialDesign:TextFieldAssist.HasClearButton="True"
                     materialDesign:HintAssist.HintPaddingBrush="White"
                     materialDesign:HintAssist.FloatingScale="1"
                     FontSize="14"
                     AcceptsReturn="True"
                     Width="217" 
                     Style="{StaticResource MaterialDesignOutlinedTextBox}">
                <TextBox.Text>
                    <Binding
                        Path="SelectedFinanciallyResponsiblePerson.ShortName"
                        Mode="TwoWay"
                        UpdateSourceTrigger="PropertyChanged">
                    </Binding>
                </TextBox.Text>
                <b:Interaction.Behaviors>
                    <behaviors:MouseClickCommandBehavior Command="{Binding SelectFinanciallyResponsiblePersonCommand}" />
                </b:Interaction.Behaviors>
            </TextBox>
            <DatePicker FontSize="14"
                        SelectedDate="{Binding WorkDateTime}" 
                        Margin="0,0,0,20" 
                        Width="217" 
                        Height="52"
                        materialDesign:HintAssist.FloatingScale="1"
                        materialDesign:HintAssist.HintPaddingBrush="White"
                        materialDesign:HintAssist.Hint="Дата"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
            <Button Content="Эксплуатация" Command="{Binding MaterialsInUseCommand}" Width="200" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
